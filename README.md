# mySaasProject
## Как это же работает?
Нужно запускать через `docker compose up`, в docker-compose.yml есть нужные сервисы(контейнер), 
хочешь запустить отдельно 1 сервис(контейнер) `docker compose "имя сервиса"`
